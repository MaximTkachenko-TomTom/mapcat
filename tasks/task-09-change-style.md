# Task

Change the map style.

# Context

Currently the default map style is used on the html page. Change it to the TomTom style, like in the project `/Users/tkachenk/dev/workspace/map-playground`.

# Complication

Using TomTom style requires a TomTom API key. User shall be asked for this key at first MapCat launch and key shall be used for subsequent runs. Do not hardcode API key in the source code.

User shall be able to use MapCat with default style (like now) without API key.

