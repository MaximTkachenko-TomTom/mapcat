# Add Z-Order support

Now the iteams are drawn on the map in order of log messages apperaing in the log. But some entities must always be drawn below other ones.

For for example: a polygon that represents "feasible road lanes" must always be below the polylines that represent "recommended road lanes".
