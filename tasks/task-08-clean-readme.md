# Cleanup REAMDE file

Update README file to follow this structure.

```
mapcat
---
<Short intro text>. Signify that mapcat can be used as a repl, or take commands form stdin.

Usage
---
Show examplex of usages as repl and in pipe mode.
mapcat
<include examples of commands>

echo "command" | mapcat

cat commands.txt | mapcat

adb connection
---
Explain that commands can be produced by `adb logcat` too, show usage example.

Installation
---
<installation steps>
```

